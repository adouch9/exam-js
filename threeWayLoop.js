const tab = ['Alice', 'Bob', 'Charlie', 'Dan', 'Eve']

                // Boucle for

for(i = 0; i < tab.length; i++){

console.log(`${tab[i]}, length:${tab[i].length}`)

}

                 //  Boucle for of

for (const elem of ['Alice', 'Bob', 'Charlie', 'Dan', 'Eve']){
  console.log(`${elem}, length:${elem.length}`)
}

                  // boucle forEach

['Alice', 'Bob', 'Charlie', 'Dan', 'Eve'].forEach((elem) => {
console.log(`${elem}, length:${elem.length}`)
})








//   console.log(`${tab[0]} , ${tab[0].length}`)
//   console.log(`${tab[1]} , ${tab[1].length}`)
//   console.log(`${tab[2]} , ${tab[2].length}`)
//   console.log(`${tab[3]} , ${tab[3].length}`)
//   console.log(`${tab[4]} , ${tab[4].length}`)

